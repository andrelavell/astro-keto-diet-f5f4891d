---
interface Props { tag: string }
const { tag } = Astro.props as Props;
---
<a href={`/tags/${encodeURIComponent(tag)}`} class="rounded-md bg-zinc-100 px-2 py-1 text-xs font-medium text-zinc-700 transition hover:bg-zinc-200 dark:bg-zinc-800 dark:text-zinc-200 dark:hover:bg-zinc-700">#{tag}</a>
